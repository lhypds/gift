{
    "description": "List GitHub Pull Requests by a chosen week, grouped by date.",
    "options": [
        {
            "key": "repos",
            "env": "GIFT_REPOS",
            "type": "string",
            "description": "Repositories to search when --repos is not given, comma separated: owner/repo1,owner/repo2"
        },
        {
            "key": "author",
            "env": "GIFT_AUTHOR",
            "type": "string",
            "description": "GitHub login whose pull requests are listed when --author is not given."
        }
    ]
}
