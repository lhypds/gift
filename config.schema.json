{
    "description": "Shared settings and the hooks server itself. Each trigger's own settings live under triggers.<type>.",
    "options": [
        {
            "key": "pm2_name",
            "env": "PM2_NAME",
            "type": "string",
            "default": "gift",
            "description": "Name the server runs under in PM2."
        },
        {
            "key": "port",
            "env": "PORT",
            "type": "number",
            "min": 1,
            "default": 3999,
            "description": "Port the server listens on, for the dashboard and the GitHub endpoint."
        },
        {
            "key": "serve_host",
            "env": "GIFT_SERVE_HOST",
            "type": "string",
            "default": "127.0.0.1",
            "description": "Address the server binds."
        },
        {
            "key": "serve_log",
            "env": "GIFT_SERVE_LOG",
            "type": "string",
            "default": "hooks.log",
            "description": "Where the server appends its log. A relative path is relative to the project folder; `off` keeps everything on the console."
        }
    ]
}
